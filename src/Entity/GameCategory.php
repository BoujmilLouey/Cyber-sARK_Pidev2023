<?php

namespace App\Entity;

use App\Repository\GameCategoryRepository;
use Doctrine\ORM\Mapping as ORM;
use Doctrine\Common\Collections\ArrayCollection;
use Doctrine\Common\Collections\Collection;
use Symfony\Component\Validator\Constraints as Assert;


#[ORM\Entity(repositoryClass: GameCategoryRepository::class)]
class GameCategory
{
    #[ORM\Id]
    #[ORM\GeneratedValue]
    #[ORM\Column]
    private ?int $id = null;

    #[ORM\Column(length: 255)]
    #[Assert\Length(min:4,minMessage:"Veuillez entrer un nom de minimum 4 caractères")]
    #[Assert\NotBlank(message:"Le nom est requis")]
     private ?string $name = null;

    #[ORM\OneToMany(mappedBy: 'gameCategory', targetEntity: Game::class)]
    private Collection $games;

    public function __construct()
    {
        $this->games = new ArrayCollection();
    }

    public function getId(): ?int
    {
        return $this->id;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    public function setName(string $name): self
    {
        $this->name = $name;

        return $this;
    }

    /**
     * @return Collection<int, Game>
     */
    public function getGames(): Collection
    {
        return $this->games;
    }

    public function addGame(Game $game): self
    {
        if (!$this->games->contains($game)) {
            $this->games->add($game);
            $game->setGameCategorie($this);
        }

        return $this;
    }

    public function removeGame(Game $game): self
    {
        if ($this->games->removeElement($game)) {
            // set the owning side to null (unless already changed)
            if ($game->getGameCategorie() === $this) {
                $game->setGameCategorie(null);
            }
        }

        return $this;
    }
    public function showGamesByCategory(gameCategory $gameCategory)
{
    // Your DQL query to fetch games in the given category
}
}
