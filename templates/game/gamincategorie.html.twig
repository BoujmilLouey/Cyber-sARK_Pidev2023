{% extends 'base_BACK.html.twig' %}
{% block title %}{% endblock %}


{% block body %}

	<h1 class="nk-feature-title">
		Games in categorie
	</h1>
</table>
<table>
	<thead></thead>
	<tbody>
		{% for game in games %}
			<tr>
				<td>
					<a
						class="nk-product-image" href="store-product.html">{# <img src="{{ asset(game.image, 'game_images_directory') }}" alt="My Image" style="width: 120px; height: 160px;"> #}
					</a>
				</td>
				<td>

					<h3 class="nk-feature-title">
						<a>{{ game.name}}</a>
					</h3>
				</td>


				<td>
					<a href="{{ path('app_game_edit', {'id': game.id}) }}">
						<button class="btn-btn-primary">edit</button>
					</a>
				</td>


				<td>
					{{ include('game/_delete_form.html.twig') }}</td>


			</tr>


		{% endfor %}
	</tbody>
</table>{% endblock %}
