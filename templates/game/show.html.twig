{% extends 'base_BACK.html.twig' %}

{% block title %}Game{% endblock %}

{% block body %}
    <h1>Game</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game.id }}</td>
            </tr>
            <tr>
                <th>Nom_jeux</th>
                <td>{{ game.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td> <img style="width: 200px;" src="{{ asset('FrontOffice/assets/images/' ~ game.image) }}">

                    </img></td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ game.description }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ game.gameCategorie.name }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_game_index') }}">back to list</a>

    <a href="{{ path('app_game_edit', {'id': game.id}) }}">edit</a>

    {{ include('game/_delete_form.html.twig') }}
{% endblock %}
